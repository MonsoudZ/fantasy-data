# Core runtime dependencies are declared in pyproject.toml ([project.dependencies]),
# the single source of truth (with version ceilings for reproducibility). This
# file installs them so `pip install -r requirements.txt` keeps working.
#
# Optional extras live there too:
#   pip install -e ".[neural]"   # the GRU projector (PyTorch)
#   pip install -e ".[web]"      # the FastAPI web UI
-e .
